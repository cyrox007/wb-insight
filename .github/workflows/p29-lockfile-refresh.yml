name: P29 lockfile refresh (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary P29 lockfile regeneration workflow is disabled; package-lock.json is committed and normal CI is authoritative.'
